# DEPRECATED: Replaced by voxchain-ingress.yaml (nginx + cert-manager + Let's Encrypt).
# Delete this file after confirming voxchain-ingress.yaml works.
# Old GCE Ingress kept for reference only — do NOT apply.
