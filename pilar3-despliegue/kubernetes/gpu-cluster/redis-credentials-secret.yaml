apiVersion: v1
kind: Secret
metadata:
  name: redis-credentials
  namespace: g-git-push-cv
type: Opaque
stringData:
  # Misma contraseña que el Redis del namespace voxchain (A-02). Se llena en CI/CD
  # desde GitHub Secrets / GCP Secret Manager (clave redis-pass). El pool-coordinator
  # la usa para el lease pool:leader sobre redis.voxchain.svc.cluster.local.
  password: ""
