apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: nct
  namespace: voxchain
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: nct-primary
  namespaceSelector:
    matchNames:
      - voxchain
  endpoints:
    - port: health
      path: /metrics
      interval: 15s
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: nct-standby
  namespace: voxchain
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: nct-standby
  namespaceSelector:
    matchNames:
      - voxchain
  endpoints:
    - port: health
      path: /metrics
      interval: 15s
